export default defineEventHandler(async (event) => {
    const config = useRuntimeConfig()

    let api_url = 'https://api.weather.gov/points/42.248344378444216,-83.01394935422068'
    let weather = {}

    return weather
})